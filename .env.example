# === AI (Gemini, gratis) ===
GEMINI_API_KEY=your_gemini_api_key_here
# Model klasifikasi (opsional, default gemini-2.5-flash)
GEMINI_MODEL=gemini-2.5-flash
# Jeda antar panggilan klasifikasi (detik) agar tidak kena limit per-menit.
# Naikkan jika masih sering kena error 429; turunkan jika billing sudah aktif.
GEMINI_CALL_DELAY_SECONDS=13

# === Telegram Bot ===
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
TELEGRAM_CHAT_ID=your_telegram_chat_id_here

# === Dashboard ===
# URL publik dashboard (dicantumkan di laporan Telegram). Isi setelah deploy.
DASHBOARD_URL=

# === Sumber Scraper (multi-sumber) ===
# Sumber yang diaktifkan, dipisah koma. Pilihan: olx, threads, facebook
ENABLED_SCRAPERS=olx,threads,facebook
# Maksimum item per sumber per run
SCRAPER_LIMIT=25

# Threads (Meta) — kata kunci pencarian (dipisah koma). PENDEK lebih efektif
# daripada frasa panjang & spesifik (terbukti lewat pengujian live).
THREADS_KEYWORDS=rumah sidoarjo,rumah surabaya,jual rumah surabaya,cari rumah surabaya,ruko sidoarjo,tanah sidoarjo,kos surabaya,apartemen surabaya

# Facebook Group (sumber PEMBELI terkaya) — TIDAK di-scrape otomatis (alasan
# privasi, lihat scraper/facebook_scraper.py). Forward listing menarik secara
# manual ke bot Telegram. Daftar grup di config.py FB_GROUPS hanya untuk
# ditampilkan sebagai link cepat di /help bot.

# === Opsi developer ===
# Set 1 hanya untuk testing lokal agar memakai data contoh saat scraper diblokir.
# WAJIB 0 (atau kosong) di produksi supaya data palsu tidak masuk database.
USE_MOCK_DATA=0

# === Google Sheets (opsional, untuk backup/akses dari mana saja) ===
GOOGLE_CREDENTIALS_FILE=credentials.json
GOOGLE_SHEET_URL=
